# Ansible managed: Do not edit this file directly!
# The Ansible template will replace the entire contents of this file during deployment.

# Staging environment settings:

# The ansible template should include the following keys, if applicable. The key values are examples only.

# Basic CMS User Credentials before https://github.com/ualbertalib/library-cms/pull/625
# cms_user: 'admin'
# cms_password: 'mysecretpassword'

# Rollbar configuration
# rollbar_access_token: <%= ENV['ROLLBAR_ACCESS_TOKEN'] %>